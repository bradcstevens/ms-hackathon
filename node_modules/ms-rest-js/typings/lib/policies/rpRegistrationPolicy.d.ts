import { HttpOperationResponse } from "../httpOperationResponse";
import { WebResource } from "../webResource";
import { BaseRequestPolicy, RequestPolicy, RequestPolicyCreator, RequestPolicyOptions } from "./requestPolicy";
export declare function rpRegistrationPolicy(retryTimeout?: number): RequestPolicyCreator;
export declare class RPRegistrationPolicy extends BaseRequestPolicy {
    readonly _retryTimeout: number;
    constructor(nextPolicy: RequestPolicy, options: RequestPolicyOptions, _retryTimeout?: number);
    sendRequest(request: WebResource): Promise<HttpOperationResponse>;
}
